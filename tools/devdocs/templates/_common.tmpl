{{define "error-header" -}}{{ "Error" | error }}: {{ . }}{{end}}
{{define "warning-header" }}{{ "Warning" | warning }}: {{ . }}{{end}}

{{define "error-footer" -}}
    {{- if .Verbose -}}
        {{ printf "%s\n%s"
            ("Error details:" | dim)
            (inspect .Full | indent 2) }}
    {{- else -}}
        {{ `Rerun with "--verbose" to view error details.` | dim }}
    {{- end -}}
{{- end}}

